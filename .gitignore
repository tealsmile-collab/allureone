# Local secrets — copy config.example.php to config.php
/config.php

# Composer (vendor/ is committed for Hostinger auto-deploy — always use: composer install --prefer-dist)
/composer.phar
/vendor/**/.git

# PHP error log (local / may contain sensitive data)
/php_errors.log
